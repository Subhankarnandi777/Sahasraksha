diff --git a/frontend/src/components/BottomNav.jsx b/frontend/src/components/BottomNav.jsx
index 2c1c10d..46ed7bb 100644
--- a/frontend/src/components/BottomNav.jsx
+++ b/frontend/src/components/BottomNav.jsx
@@ -13,14 +13,27 @@ function Icon({ name }) {
 
 export default function BottomNav({ active, alertCount = 0 }) {
   return (
-    <nav className="bottom-nav" aria-label="Primary">
-      {items.map((item) => (
-        <a key={item.href} href={item.href} className={active === item.label.toLowerCase() ? "active" : ""}>
-          <Icon name={item.icon} />
-          <span>{item.label}</span>
-          {item.label === "Alerts" && alertCount > 0 ? <b>{alertCount}</b> : null}
-        </a>
-      ))}
-    </nav>
+    <>
+      <nav className="bottom-nav" aria-label="Primary">
+        {items.map((item) => (
+          <a key={item.href} href={item.href} className={active === item.label.toLowerCase() ? "active" : ""}>
+            <Icon name={item.icon} />
+            <span>{item.label}</span>
+            {item.label === "Alerts" && alertCount > 0 ? <b>{alertCount}</b> : null}
+          </a>
+        ))}
+      </nav>
+      {/* Desktop-only equivalent -- a standard top nav row, shown instead
+          of the floating mobile tab bar above 900px (see styles.css). */}
+      <nav className="top-nav" aria-label="Primary">
+        {items.map((item) => (
+          <a key={item.href} href={item.href} className={active === item.label.toLowerCase() ? "active" : ""}>
+            <Icon name={item.icon} />
+            <span>{item.label}</span>
+            {item.label === "Alerts" && alertCount > 0 ? <b>{alertCount}</b> : null}
+          </a>
+        ))}
+      </nav>
+    </>
   );
 }
diff --git a/frontend/src/styles.css b/frontend/src/styles.css
index 2159f5e..205a721 100644
--- a/frontend/src/styles.css
+++ b/frontend/src/styles.css
@@ -60,44 +60,7 @@ p {
    website. This instead widens into a genuine desktop dashboard: a wide,
    centred panel that actually uses the available width, with card grids
    that expand into more columns rather than staying phone-narrow. */
-@media (min-width: 900px) {
-  body {
-    background: #eef2f7;
-  }
-
-  .phone-shell {
-    max-width: 1180px;
-    margin: 0 auto;
-    background: #f7f8ff;
-    box-shadow: 0 1px 0 rgba(15, 23, 42, 0.04);
-  }
-
-  .screen {
-    padding: 28px 40px 40px;
-  }
 
-  .metric-grid.two {
-    grid-template-columns: repeat(4, 1fr);
-  }
-
-  .metric-grid.three,
-  .telemetry-mini-grid {
-    grid-template-columns: repeat(3, 1fr);
-    max-width: 720px;
-  }
-
-  .content-grid,
-  .dashboard-screen > .card,
-  .dashboard-screen > .hero-card,
-  .dashboard-screen > .photo-card {
-    max-width: 900px;
-  }
-
-  .map-panel,
-  .leaflet-container {
-    max-width: 900px;
-  }
-}
 
 .screen {
   position: relative;
@@ -873,7 +836,7 @@ p {
 
 .live-stream {
   gap: 12px;
-  margin: -28px 14px 22px;
+  margin: 12px 14px 22px;
   padding: 12px;
 }
 
@@ -1293,3 +1256,76 @@ p {
     grid-template-columns: 1fr;
   }
 }
+
+/* Desktop presentation: a genuine website layout, not a phone app centred
+   in a browser. The floating bottom tab bar -- the single biggest "mobile
+   app" signal -- is replaced by a standard top navigation row above 900px,
+   and the content column is widened enough that the three-way telemetry
+   cards (Temperature/Pressure/Humidity) actually fit without clipping. */
+.top-nav {
+  display: none;
+}
+
+@media (min-width: 900px) {
+  body {
+    background: #eef2f7;
+    display: flex;
+    justify-content: center;
+  }
+
+  .phone-shell {
+    width: 760px;
+    margin: 0 auto;
+    background: #f7f8ff;
+    box-shadow: 0 0 0 1px rgba(15, 23, 42, 0.06);
+  }
+
+  .screen {
+    padding: 96px 32px 40px;
+  }
+
+  .bottom-nav {
+    display: none;
+  }
+
+  .top-nav {
+    display: flex;
+    position: fixed;
+    top: 0;
+    width: 760px;
+    justify-content: center;
+    gap: 8px;
+    padding: 14px 24px;
+    background: #ffffff;
+    border-bottom: 1px solid rgba(210, 218, 232, 0.9);
+    z-index: 10;
+  }
+
+  .top-nav a {
+    display: flex;
+    align-items: center;
+    gap: 6px;
+    padding: 8px 16px;
+    border-radius: 10px;
+    color: #64748b;
+    font-size: 13px;
+    font-weight: 700;
+  }
+
+  .top-nav a.active {
+    color: #0b82ad;
+    background: #eef9ff;
+  }
+
+  .top-nav b {
+    display: inline-grid;
+    place-items: center;
+    min-width: 16px;
+    height: 16px;
+    padding: 0 4px;
+    border-radius: 999px;
+    background: #dc2626;
+    color: #ffffff;
+    font-size: 10px;
+  }
+}
